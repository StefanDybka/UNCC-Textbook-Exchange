<div class="container">
    <div class="row shadow bg-white justify-content-center p-3 m-4">
        <div class="row">
            <h1>Name:</h1>
            <%= @listing.name %>
        </div>

        <div class="row">
            <h1>ISBN:</h1>
            <%= @listing.isbn %>
        </div>

        <div class="row">
            <h1>Email:</h1>
            <%= @listing.email %>
        </div>

        <div class="row">
            <h1>Condition:</h1>
            <%= @listing.condition %>
        </div>
    </div>
</div>